# config.py - 评分卡模型权重配置
# 所有权重集中管理，支持调参

# ==========================================
# 双色球 - 红球因子权重（33选6）
# ==========================================
SSQ_RED_WEIGHTS = {
    'repeat': 3.0,       # 重号
    'neighbor': 2.0,     # 邻号
    'sum_balance': 2.0,  # 和值平衡
    'zone_balance': 2.0, # 区间平衡
    'pattern': 1.0,      # 历史模式
}

# ==========================================
# 双色球 - 蓝球因子权重（16选1）
# ==========================================
SSQ_BLUE_WEIGHTS = {
    'recent_hot': 2.0,   # 近期热度
    'history_freq': 1.0, # 历史频率
    'repeat': 1.5,       # 重号
    'neighbor': 1.0,     # 邻号
}

# ==========================================
# 双色球 - 结构模板（基于3447期真实数据）
# (和值, 奇偶, 跨度, 三区)
# ==========================================
SSQ_STRUCT_TEMPLATES = [
    ((90, 110), (2, 4), (18, 28), (2, 2, 2)),    # 15.4%
    ((90, 110), (2, 4), (18, 28), (3, 2, 1)),    # 9.4%
    ((80, 100), (2, 4), (18, 28), (3, 1, 2)),    # 9.3%
    ((100, 120), (2, 4), (18, 28), (1, 3, 2)),   # 9.1%
    ((90, 110), (2, 4), (18, 28), (1, 2, 3)),    # 8.6%
    ((90, 110), (2, 4), (18, 28), (1, 3, 2)),    # 7.4%
    ((80, 100), (2, 4), (18, 28), (2, 1, 3)),    # 6.8%
    ((120, 140), (2, 4), (18, 28), (3, 2, 1)),   # 6.1%
]

# ==========================================
# 大乐透 - 前区因子权重（35选5）
# ==========================================
DLT_FRONT_WEIGHTS = {
    'repeat': 3.0,
    'neighbor': 2.0,
    'sum_balance': 2.0,
    'zone_balance': 2.0,
}

# ==========================================
# 大乐透 - 后区Pair评分权重（12选2）
# ==========================================
DLT_BACK_PAIR_WEIGHTS = {
    'recent_hot': 2.0,       # 近20期出现次数
    'mid_freq': 0.1,         # 近100期频率（cnt/10）
    'consecutive': 2.0,      # 是否上期pair
    'neighbor': 1.0,         # 邻号命中
    'span_small': 0.8,       # 跨度1-5
    'span_mid': 0.4,         # 跨度6-8
    'sum_mid': 0.5,          # 和值7-14
}

# ==========================================
# 大乐透 - 前区结构模板
# (和值, 奇偶, 跨度, 三区)
# ==========================================
DLT_FRONT_STRUCT_TEMPLATES = [
    ((70, 110), (2, 4), (18, 30), (2, 1, 2)),    # 14.8%
    ((70, 110), (2, 4), (18, 30), (2, 2, 1)),    # 13.0%
    ((70, 110), (2, 4), (18, 30), (1, 2, 2)),    # 11.7%
    ((80, 120), (2, 4), (20, 30), (1, 3, 1)),    # 8.7%
    ((70, 100), (3, 5), (20, 28), (1, 1, 3)),    # 8.4%
    ((60, 90),  (2, 3), (18, 26), (3, 1, 1)),    # 6.8%
    ((80, 110), (2, 3), (22, 30), (2, 0, 3)),    # 5.1%
    ((60, 90),  (2, 4), (20, 28), (0, 2, 3)),    # 4.8%
]

# ==========================================
# 大乐透 - 后区结构模板
# ==========================================
DLT_BACK_STRUCT_TEMPLATES = [
    ((8, 18), (1, 1), (1, 7)),   # 1奇1偶 + 小跨度
    ((6, 16), (0, 0), (1, 5)),   # 全偶
    ((10, 20), (2, 2), (1, 7)),  # 全奇
    ((8, 18), (1, 1), (1, 5)),   # 最密集
]

# ==========================================
# 双色球 - 回测参数
# ==========================================
SSQ_BACKTEST = {
    'top_n_candidates': 20,      # 候选号码数量
    'min_train_draws': 50,       # 最小训练期数
    'consecutive_max_reds': 3,   # 与前一期最大重复红球数
    'random_seed': 42,           # 随机种子（如使用）
}

# ==========================================
# 大乐透 - 回测参数
# ==========================================
DLT_BACKTEST = {
    'front_top_n': 15,
    'back_top_n_pairs': 25,
    'min_train_draws': 30,
    'max_repeat_fronts': 3,
    'random_seed': 42,
}
